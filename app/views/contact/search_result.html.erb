<h1>Results for the search: <%= @variable %></h1>

<ul>
  <% @contacts.each do |contact| %>
  <li>
    <%= link_to contact.name, "/#{contact.id}"%>
  </li>
  <%end%>
</ul>

<%= link_to "New Contact", "/new_contact" %>
<%= link_to "Home", "/" %>
