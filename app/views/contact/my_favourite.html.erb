<h1>Favourites Contacts</h1>

<ul>
  <% @fav_array.each do |contact| %>
  <li>

    <%= link_to contact.name, "/#{contact.id}"%>


  </li>
  <%end%>
</ul>

<%= link_to "New Contact", "/new_contact" %>
<%= link_to "Home", "/" %>
